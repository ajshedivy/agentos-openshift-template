# ============================================================
# IBM i Connection (Required)
# ============================================================
# Your IBM i system hostname or IP address
DB2_HOST=your_ibmi_hostname
# IBM i user profile with appropriate authorities
DB2_USER=your_ibmi_user
# Password for the IBM i user profile
DB2_PASSWORD=your_ibmi_password
# Database name (default: *LOCAL for local system)
DB2_DATABASE=*LOCAL
# Port (default: 8076)
DB2_PORT=8076

# ============================================================
# MCP Server Configuration (Optional)
# ============================================================
# HTTP port for the IBM i MCP server (default: 3010)
MCP_HTTP_PORT=3010
# Transport type (default: streamable-http)
MCP_TRANSPORT_TYPE=streamable-http

# ============================================================
# Database Configuration (Required for Gateway)
# ============================================================
# PostgreSQL database URL (format: postgresql://user:password@host:port/database)
# For local Docker Compose, use container service names
DATABASE_URL=postgresql://postgres:mysecretpassword@postgres:5432/mcp

# Alternate database options (uncomment ONE):
# DATABASE_URL=mysql+pymysql://mysql:changeme@mysql:3306/mcp
# DATABASE_URL=mysql+pymysql://admin:changeme@mariadb:3306/mcp
# DATABASE_URL=mongodb://admin:changeme@mongodb:27017/mcp

# ============================================================
# Cache Configuration (Required for Gateway)
# ============================================================
# Redis cache URL (format: redis://host:port/db)
REDIS_URL=redis://redis:6379/0
CACHE_TYPE=redis

# ============================================================
# JWT Authentication (Required for Gateway)
# ============================================================
# JWT algorithm (HS256 for HMAC, RS256 for RSA)
JWT_ALGORITHM=HS256
# JWT secret key (for HMAC algorithms) - CHANGE THIS IN PRODUCTION
JWT_SECRET_KEY=my-test-key
# JWT issuer and audience
JWT_ISSUER=mcpgateway
JWT_AUDIENCE=mcpgateway-api

# ============================================================
# Platform Admin (Required for Gateway)
# ============================================================
# Default platform admin credentials
PLATFORM_ADMIN_EMAIL=admin@example.com
PLATFORM_ADMIN_PASSWORD=changeme

# ============================================================
# Application Configuration (Optional)
# ============================================================
# Server host and port
HOST=0.0.0.0
PORT=4444
# Environment (development, staging, production)
ENVIRONMENT=development
# Enable features
EMAIL_AUTH_ENABLED=true
MCPGATEWAY_UI_ENABLED=true
MCPGATEWAY_ADMIN_API_ENABLED=true
# Security
SECURITY_HEADERS_ENABLED=true
CORS_ALLOW_CREDENTIALS=true
REQUIRE_TOKEN_EXPIRATION=false

# ============================================================
# Database Passwords (Optional - for docker-compose services)
# ============================================================
# These are used by the database containers themselves
POSTGRES_PASSWORD=mysecretpassword
MYSQL_PASSWORD=changeme
MARIADB_PASSWORD=changeme
MONGO_PASSWORD=changeme
